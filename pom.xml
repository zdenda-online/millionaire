<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cz.dix</groupId>
    <artifactId>millionaire</artifactId>
    <version>1.0</version>

    <name>Millionaire game</name>
    <description>
        Game that was developed as a gift for my family.
        It is based on "Who Wants to Be a Millionaire?" TV show and it allows to play it in your living room.
        Printing "money-like" paper checks and proper moderator role-playing can provide a lot of fun:-)
    </description>

    <developers>
        <developer>
            <name>Zdenek Obst</name>
            <email>zdenek.obst-at-gmail.com</email>
        </developer>
    </developers>
    <scm>
        <url>git@github.com:d1x/millionaire.git</url>
        <connection>scm:git:git@github.com:d1x/millionaire.git</connection>
        <developerConnection>scm:git:git@github.com:d1x/millionaire.git</developerConnection>
    </scm>

    <properties>
        <compiler.version>1.7</compiler.version>
        <assembly.dir>${project.build.directory}/${project.artifactId}</assembly.dir>
        <archive.name>${project.artifactId}-${project.version}</archive.name>
        <game.name>${project.artifactId}</game.name>
        <game-creator.name>${project.artifactId}-game-creator</game-creator.name>

        <jtatto.version>1.6.7</jtatto.version>
        <jmf.version>2.1.1e</jmf.version>
        <junit.version>4.11</junit.version>
        <mvn.compiler.version>3.1</mvn.compiler.version>
        <mvn.appassembler.version>1.7</mvn.appassembler.version>
        <mvn.antrun.version>1.7</mvn.antrun.version>

        <project.build.encoding>UTF-8</project.build.encoding>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.jtattoo</groupId>
            <artifactId>JTattoo</artifactId>
            <version>${jtatto.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>javax.media</groupId>
            <artifactId>jmf</artifactId>
            <version>${jmf.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${mvn.compiler.version}</version>
                <configuration>
                    <source>${compiler.version}</source>
                    <target>${compiler.version}</target>
                </configuration>
            </plugin>

            <!-- Generate program structure (with runnable scripts) -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>appassembler-maven-plugin</artifactId>
                <version>${mvn.appassembler.version}</version>
                <configuration>
                    <repositoryName>lib</repositoryName>
                    <repositoryLayout>flat</repositoryLayout>
                    <configurationSourceDirectory>src/main/games</configurationSourceDirectory>
                    <configurationDirectory>games</configurationDirectory>
                    <copyConfigurationDirectory>true</copyConfigurationDirectory>
                    <includeConfigurationDirectoryInClasspath>false</includeConfigurationDirectoryInClasspath>
                    <assembleDirectory>${assembly.dir}</assembleDirectory>
                    <extraJvmArguments>-Xms128m -Xmx256m</extraJvmArguments>
                    <showConsoleWindow>false</showConsoleWindow>
                    <platforms>
                        <platform>unix</platform>
                        <platform>windows</platform>
                    </platforms>
                    <binFileExtensions>
                        <unix>.sh</unix>
                        <windows>.bat</windows>
                    </binFileExtensions>
                    <programs>
                        <program>
                            <id>${game.name}</id>
                            <mainClass>cz.dix.mil.GameMain</mainClass>
                        </program>
                        <program>
                            <id>${game-creator.name}</id>
                            <mainClass>cz.dix.mil.CreatorMain</mainClass>
                        </program>
                    </programs>
                </configuration>
                <executions>
                    <execution>
                        <id>appassembler</id>
                        <phase>package</phase>
                        <goals>
                            <goal>assemble</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Change permissions for scripts and compress to distributable archives (zip/tar.gz) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>${mvn.antrun.version}</version>
                <configuration>
                    <target>

                        <chmod dir="${assembly.dir}/bin" includes="**/*.sh,**/*.bat" perm="755"/>

                        <zip destfile="${project.build.directory}/${archive.name}.zip">
                            <zipfileset dirmode="775" filemode="775" dir="${assembly.dir}/bin" prefix="bin"/>
                            <zipfileset dirmode="775" filemode="664" dir="${assembly.dir}/games" prefix="games"/>
                            <zipfileset dirmode="775" filemode="664" dir="${assembly.dir}/lib" prefix="lib"/>
                        </zip>

                        <tar destfile="${project.build.directory}/${archive.name}.tar">
                            <tarfileset dirmode="775" filemode="775" dir="${assembly.dir}/bin" prefix="bin"/>
                            <tarfileset dirmode="775" filemode="664" dir="${assembly.dir}/games" prefix="games"/>
                            <tarfileset dirmode="775" filemode="664" dir="${assembly.dir}/lib" prefix="lib"/>
                        </tar>
                        <gzip destfile="${project.build.directory}/${archive.name}.tar.gz"
                              src="${project.build.directory}/${archive.name}.tar"/>
                        <delete file="${project.build.directory}/${archive.name}.tar"/>

                    </target>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>